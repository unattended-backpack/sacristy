# Local configuration.
.env

# Generated by `make genesis`. The L2 chain id, alloc, and timestamp
# pinned here are seeded into the on-chain SigilVerifier at deploy
# time, so the file is per-deployment and never shared in the repo.
genesis.json

# Build artifacts.
kurtosis-artifacts/
out/
cache/
dependencies/
broadcast/

# Editor artifacts.
.claude/
.idea/
.vscode/
*.swp
*~
